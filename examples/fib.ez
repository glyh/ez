extern unit print_int(i64 num);

i64 fib(i64 n) {
  if(n == 0) {
    return 0;
  } else if (n == 1) {
    return 1;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

i64 main() {
  print_int(fib(7));
  return 0;
}
